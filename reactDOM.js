const render = (creatingTagName, tagNameToPut, c) => {
  const root = document.getElementById(tagNameToPut);
  const tagName = document.createElement(creatingTagName);
  root.textContent = document.createElement(creatingTagName);
};

class React {
  constructor() {
    this.tagName = tagName;
  }

  createElement(tagName, property) {}
}

class ReactDOM {
  constructor() {
    this.createdElement = createdElement;
    this.target = target;
  }

  render(createElement, target) {}
}
